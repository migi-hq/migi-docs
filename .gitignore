# ─────────────────────────────────────────────────────────
# 三個程式 repo 各自獨立版控，不可納入本 repo
# 它們在 GitHub 上是 migi-hq/migi-pos、migi-web、migi-admin
# migi-assets 是三端共用的設計 token 與品牌美術（**公開** repo）
# ─────────────────────────────────────────────────────────
/migi-pos/
/migi-web/
/migi-admin/
/migi-assets/
# 官網（migi.tw）—— 2026-09-05 新增，第五個 repo
/migi-site/

# Claude Code 的本機設定（含個人權限選擇，不共用）
/.claude/settings.local.json

# 🔴 MCP 設定：裡面有 Supabase Personal Access Token，絕不可進版控。
#   （這個 repo 是私有的，但 token 進 git 之後就永遠在歷史裡，
#     即使之後刪掉那一行也還在，只能整個 token 作廢重發。）
/.mcp.json

# 作業系統與編輯器雜物
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~

# 暫存
/tmp/
*.tmp
