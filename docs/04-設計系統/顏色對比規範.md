# MIGI 顏色對比規範（WCAG AA）

> **這是什麼**：哪一個 token 可以拿來當文字、站在哪一種底上、保證多少對比。
> **什麼時候讀它**：挑文字顏色之前，以及新增任何色票之前。
>
> 🔴 **這份文件不列色值。** 值的唯一來源是 `migi-assets/tokens.json`
> （硬規則 13）。這裡只寫**值放不下的東西：規則與保證**。
> ⚠ 抄一份值進來就是這個專案記過六次的病 ——
> 而 `設計Token規範.md` 2026-09-09 之前正是那樣，於是它的灰階值漂了兩版沒人發現。

---

## 一、立場：內建，不是檢查

世界級的設計系統都參考 WCAG，但**差別在它出現在流程的哪一段**：

| | 做法 | 代價 |
|---|---|---|
| 一般 | 做完了跑工具掃一遍再修 | 一次 95 個修改點，散在五個頁面 |
| **世界級** | **寫進色票**：挑顏色那一刻就決定了 | 一次，之後每個畫面自動符合 |

```
Shopify Polaris   每個文字 token 標註它保證的對比級別
IBM Carbon        色票文件直接列對比值，另有 $link-primary 這種「文字專用」色
Google Material   tonal palette，深淺是算出來的不是挑的
Atlassian         token 分 text / background / border 三族
```

🎯 **重點不是「他們比較有良心」，是那樣比較便宜。**

⚠ **MIGI 沒有法規義務**（台灣的無障礙規範強制的是政府網站）。
這份文件的理由是實務的：

| | 為什麼在意 |
|---|---|
| 官網 `migi.tw` | 陌生人唯一會看到的那一個，也是送廣告審核時被看的 |
| 會員 App | **客群年紀偏大** ＋ 手機常在戶外看，實際對比比你在電腦前更差 |
| POS | **唯一一個有人盯著看八小時的**，而且在麻將館的燈光下 |
| 總部後台 | 使用者是自己人，最不急 |

---

## 二、AA 的實際規則（只有三條）

對比值 = 前景與背景的相對亮度比，範圍 `1:1`（同色）到 `21:1`（純黑配純白）。

| 情況 | 門檻 |
|---|---|
| 一般文字 | **4.5:1** |
| 大字（≥24px，或 **≥18.7px 且 ≥700 粗**） | 3.0:1 |
| 非文字（icon、邊框、圖表） | 3.0:1 |

🔴 **MIGI 的小字是 11–13px，全部算「一般文字」** ⇒ 一律看 4.5。

⚠ **對比是對稱的**：`A 字在 B 底` 與 `B 字在 A 底` **是同一個數字**。
📌 2026-09-09 我一度說成「文字要深才在白底過、底要深白字才過，兩個方向互相拉扯」——
**那是錯的**，沒有拉扯。

---

## 三、角色 → token 對照（這一節才是規範本身）

**實測值，不是估的**（2026-09-09 用執行期掃描量的，方法見第五節）。

### 文字色

| token | 角色 | 白底 | `--brand-tint` | `--brand` |
|---|---|---|---|---|
| `--ink` | 主文字 | ✅ 14+ | ✅ | ✅ |
| `--gray-1` | **要讀的次要文字** | ✅ 7.13 | ✅ 6.29 | ✅ 5.33 |
| `--gray-2` | 標籤、時間戳 —— **讀得到就好** | 🟡 3.79 | 🟡 3.34 | 🔴 2.84 |
| `--hint` | **刻意退到背景**（佔位提示） | 🔴 2.19 | 🔴 1.94 | 🔴 1.64 |
| `--accent` | 🔴 **不要拿來當文字**，見下 | 🟡 4.00 | 🟡 3.53 | 🔴 2.99 |

### 🔴 規則一：要被讀的文字用 `--gray-1`，不要用 `--gray-2`

`--gray-2` 在**任何**底上都不到 4.5。它的角色是「有就好」的標籤，
**不是「客人需要讀懂的句子」**。

⚠ 特別是**站在 `--brand` 粉底上時只有 2.84** —— 連大字的 3.0 都不到。
段位 Hero 的底就是 `--brand`。

### 🔴 規則二：桃紅有兩個，選錯就不合格

```
--accent      #C2607A  淺 —— 只用在「上面不寫字」或「配深色字」的填色
                          進度條、圖表、icon、配 --ink 的膠囊
--accent-deep #974B5F  深 —— 強調文字 ＋ 承載白字的填色
                          查看 ›、+30、強調數字、通知紅點、白字膠囊
```

**判準只有一句：這塊桃紅上面有沒有要讀的東西？**

| | 對比 | |
|---|---|---|
| `--accent` 當文字（白底） | 4.00 | 🔴 |
| 白字站在 `--accent` 上 | 4.00 | 🔴 |
| `--accent-deep` 當文字（白底） | 6.02 | ✅ |
| 白字站在 `--accent-deep` 上 | 6.02 | ✅ |

⚠ **「把字改成 `--ink`」那條路是死的**：墨字站在 `--accent` 上只有 **3.50**，
比白字更糟。桃紅卡在中間，**兩邊都不夠** ⇒ 唯一的解是把底換深。

🎯 這是硬規則 13.7 的同一個判準（`--divider` 從 `--gray-4` 拆出來時用的）：
**「這個值改了，哪些東西會跟著動？」超過一種用途就該拆。**

🔴 **但也不要拆過頭。** 一開始這個 token 叫 `--accent-text`，
同日改名 `--accent-deep` —— 因為「文字」與「白字的底」**需求是同一個**
（在淺底上要夠深），不是兩種用途。
**拆了會長出第三個 accent，而那正是同一條判準的反面。**

⚠ **不要用「把 `--accent` 改深」解決** —— 那會讓進度條與圖表一起變深，
而那不是要解決的問題。品牌色是為了「好看」挑的，不是為了 4.5 挑的。

### 🔴 規則三：深色底一律配 `--white`

`--gray-1` 站在 `--ink` 上只有 **1.97**（加深之前是 3.08 —— 也不合格）。
✅ 2026-09-09 掃過三端，**「深底 ＋ 灰字」是 0 處**，這條目前沒有被違反。

### 背景色（不當文字用）

`--gray-3` / `--gray-4` / `--field-bg` / `--page-bg` / `--divider` /
`--brand-tint` / `--brand-light` —— **這些只當底或線，不要拿來寫字。**

---

## 四、刻意不修的三類（理由各不相同）

**不修 ≠ 沒看到。** 每一類的理由不一樣，混在一起會讓下一個人以為是漏掉的。

### ① `--gray-2` 白底 3.79（約 45 處）—— 修了會塌掉階梯

要過 4.5 得拉到約 `#7B7673`，而那跟 `--gray-1` 幾乎一樣
⇒ **剛做好的等距灰階就沒有中間階了**。

📌 白底上 4.5 的分界線大約在 `#7B7673`，也就是說：
**這條灰階不可能既等距、又每一階都過 AA。**
要三階全過，它們得全部擠在 `#7B7673` 到 `--ink` 之間，會長得幾乎一樣。
⇒ 現況是一個**有意識的分工**，不是妥協。

### ② LINE 綠底白字 2.26（1 處）—— 不能改

LINE 官方明文規定登入按鈕的顏色與形狀**不可修改**
（`--line-green` 的 token 描述也寫了「不是我們的設計決定」）。

### ③ `--hint` 全部不合格 —— 那是它的工作

它存在的目的就是「退到背景」（輸入框的佔位提示）。
⚠ **但不要拿它寫任何客人需要讀的東西。**

---

## 五、怎麼自己量

🔴 **要用執行期掃描，不要靜態掃描** —— 背景色常常來自祖先元素，
而 inline style 看不出來。`getComputedStyle` 才是準的。

掃描器有三件事非做不可，少一件結果就是錯的：

```
① 半透明背景要逐層混合    rgba 直接算會得到錯的比值
② 門檻依字級而定          大字 3.0、一般 4.5
③ 只看「自己有文字節點」的元素   否則父層會被重複計算
```

📌 完整實作：**`docs/_資產/contrastcheck.js`**（2026-09-10 建）。
正負對照樣本：`docs/_資產/contrastcheck_樣本.html`（每一格的預期值都是手算過的
—— 檢查器改動之後拿那一頁重跑，結果不同就是檢查器變了，不是頁面變了）。
執行方式：起 dev server → **先裝寫入攔截器**（硬規則 11.6）→ 走過五個分頁 → 收集。

🔴 **這一行在 2026-09-10 之前是假的。** 它原本寫「完整實作見 `docs/_資產/`」，
而那個資料夾裡 26 個檔案沒有一個是它 —— 2026-09-09 那次掃描是用**臨時貼進
console 的片段**做的，沒有存下來。⇒ 那些量出來的數字**沒有辦法重現**，
而**不能重現的量測等於一次性的意見**。
📌 同一天的第二次（第一次是硬規則 3.6 寫著 `jsxcomment.py` 存在，而它從來沒存在過）。
**寫「實作在某處」之前，先確認那個檔案真的在那裡。**
✅ **2026-09-10 拿樣本頁跑完了，八格全部照預期**：
```
文字 10 個  →  只抓到「一般文字未達」那一格（3.03，與手算相同）
元件  5 個  →  只抓到「邊框太淡」與「填色幾乎看不見」兩顆
不該被抓的都沒被抓：剛好過 4.54 的文字、26px 的大字（門檻 3.0）、
半透明白字疊深底、文字在子元素的父層、以及分隔線
```
🎯 **正對照與負對照都在同一頁上**，所以「它會抓」與「它不亂抓」
  是一次驗完的（硬規則 3.55）。

🔴 **而跑第一次就抓到一個錯 —— 錯的是樣本頁不是掃描器**：
  那一頁註解寫「邊框 `#EEEEEE` 對白底 = 1.13」，實際是 **1.16**。
  手算 `L(#EEE)=0.8550 → 1.05/0.9050 = 1.160`。已更正。
  ⚠ 同硬規則 3.56：**對不上時先懷疑期望值**。這一格不影響通過與否，
    但一個錯的期望值留著，下次就會有人去「修」一支對的程式。

---

## 六之一、拿去掃真的畫面（2026-09-10）

會員 App 五頁，視窗 375×812（真的目標尺寸），全程掛寫入攔截器。

| 頁 | 文字檢查 | 未達 4.5 | 元件檢查 | 未達 3.0 |
|---|---|---|---|---|
| 錢包 | 66 | 4 | 9 | 3 |
| 配桌 | 54 | 7 | 9 | 1 |
| 牌咖 | 30 | 10 | 7 | 2 |
| 成績 | 130 | 17 | 11 | 2 |
| 獎勵 | 76 | 9 | 29 | 7 |

### 🎯 文字那 47 處幾乎全是同一個顏色
**`--gray-2` #88827E 對白底 3.79、對紙底 3.54，門檻是 4.5。**
實際是哪些字：底部導覽的「配桌／成績／牌咖」、對局紀錄的日期、
任務進度的「3 / 25」、牌咖頁那句「合得來就邀成牌咖」。**全部是小字。**

```
--ink     #2E2B2C   白 14.02 · 紙 13.10 · 欄位 13.10 · 淡粉 12.36   ✅
--gray-1  #5C5754   白  7.13 · 紙  6.66 · 欄位  6.66 · 淡粉  6.29   ✅
--gray-2  #88827E   白  3.79 · 紙  3.54 · 欄位  3.54 · 淡粉  3.34   🔴 只有大字合格
--hint    #B4AEA9   白  2.19 · 紙  2.05 · 欄位  2.05 · 淡粉  1.94   🔴 大字也不合格
```
`--gray-2` 全站 288 處、`--hint` 47 處。

🔴 **建議不是再調深一次。** 09-09 才把這四階算成等距，而 `--gray-1`
  已經逼近 `--ink`，再往下調會把「主要文字」與「次要文字」的分層壓掉。
  ⇒ **讓掃描器指認哪幾處真的失敗，把那些改成 `--gray-1`，token 不動。**
    288 處裡有一部分本來就是大字，那些是合格的 —— 用眼睛分不出來，這支可以。

⚠ 另一個獨立的：**LINE 官方按鈕**（白字站在 `#06C755` 上 2.26）
  是 LINE 的品牌規範，**不可改**（硬規則：不可改形狀與顏色）。
  它會一直出現在報告裡，**那是已知例外不是待辦**。

### 🔴 而第一次掃真畫面，錯的是掃描器自己 —— 兩處，方向相反
| | 症狀 | 修法 |
|---|---|---|
| **① `cursor: pointer` 會繼承** | 一張可點的卡片裡**每一個 span 與圖都被算成元件**（錢包頁 10 個裡有 5 個是卡片內部的裝飾） | 只靠繼承拿到 pointer 的，**上面還有可點的祖先就跳過**；自己是真的互動標籤則照算 |
| **② 規範讀太寬** | 把**每一顆淺底按鈕**都報出來 | 1.4.11 管的是「**辨識元件所必需**的視覺資訊」——寫著字的按鈕是靠字被辨識的，那歸 1.4.3。⇒ **有可讀文字標籤的一律退出這一項** |

⇒ 兩處修完之後，元件那半從「錢包頁 9 處」降到 **3 處**，
  剩下的都是**沒有文字**的東西（純圖示鈕、色塊）—— 那才是真的靠形狀被辨識。
⚠ **已知還沒涵蓋**：`狀態`（選中 vs 未選中只差一個底色）要比較兩個狀態，
  掃一次靜態畫面看不出來，**這支不假裝自己驗得了**。
⚠ 以及**圖示鈕裡面那個圖示**：底色不合格但圖示本身夠深時仍會被報出來，
  那幾處要人看一眼。

### 🔴 最危險的那一個：視窗太窄會靜靜回報「零個問題」
第一次跑樣本頁時分頁寬度趨近於零 ⇒ `<p>` 量到 `0x140`、按鈕 `43x137`
（一個字一行）⇒ **每一個區塊元素都被判成看不見** ⇒ 掃描結果全綠。
⚠ **那跟「這一頁很乾淨」長得一模一樣。**
✅ 已加守衛：`innerWidth < 320` 直接**拒絕給答案**並說明原因，
  而且每次回傳都印出視窗寬 —— 那個數字決定了其他數字算不算數。

⚠ **掃描前要種一個 `is_test` 的身分** —— 本機 dev 連的是正式資料庫。

---

## 六、決策紀錄

### 2026-09-09：灰階加深並拉回等距（`@migi/assets` v1.6.0 → v1.7.0）

起因是使用者說「本季段位分」那類小字讀不清楚。

```
hint → gray-2 → gray-1 → ink        每階約 −44（實測）
```

🎯 **而它修好了一件沒有人知道壞掉的事**：加深之前 `--gray-1` 站在
`--brand-tint` / `--gray-4` / `--brand-light` 上是 **3.88–4.01**，
**未達 AA**。使用者是憑「讀不清楚」講的，而那個直覺對上了一個可以量的標準。

⚠ `--gray-2` 的值是**算出來的不是挑的**（取 `hint` 與 `gray-1` 的逐通道中點）。
🔴 **這四階要動就一起動** —— 只改其中一個就會出現「中間那一階卡住」。

### ✅ 2026-09-09：`--accent-deep` 拆出來（v1.8.0）

候選值（實測），選 `#974B5F` —— **唯一在五種底上都過的**：

| | 白 | tint | brand |
|---|---|---|---|
| `--accent` `#C2607A` | 4.00🟡 | 3.53🟡 | 2.99🔴 |
| `#B45971` | 4.56✅ | 4.02🟡 | 3.41🟡 |
| `#A64C63` | 5.48✅ | 4.83✅ | 4.10🟡 |
| **`--accent-deep` `#974B5F`** | **6.02✅** | **5.31✅** | **4.51✅** |

🎯 最難的 `--brand` 那一格**不是特例**：段位 Hero 的「再 40 分」就站在那裡，
那是成績頁第一眼最該讀得到的東西。

**成果：95 筆／20 組合 → 59 筆／8 組合**，`--accent` 當文字的失敗全部消失。

📌 預覽：`docs/_資產/accent-text候選值.html`

### 🔴 整批替換踩到的兩個坑（都是「過濾條件寫錯」）

替換 90 處時做錯兩次，**兩次都不是想法錯，是過濾器錯**：

```
① 條件用「這一行含 color」
   ⇒ 同時有 background:accent 與 color:white 的膠囊，背景也被換掉（27 → 16）
② 檢查誤改時用 grep -vi "color:" 排除
   ⇒ 而 borderTopColor: 裡面就含 Color: ⇒ 邊框那幾行被過濾掉沒顯示
   ⇒ 我因此宣告「乾淨」，而它不乾淨
```

✅ **兩次都是靠「總數要對得上」抓到的**（`78 + 79 = 157`）——
單看「有沒有出現不該出現的東西」兩次都會過。
🎯 **凡是整批替換，先記下基準總數，改完要能加回同一個數字。**

正解：**大小寫敏感 ＋ 要求 `color` 在前**的正則 ——
`backgroundColor` / `borderTopColor` 是大寫 C，**天然分得開**，
不需要另外排除（而「另外排除」正是第 ② 個坑的來源）。

### ✅ 順手修掉一個不走 token 的硬寫色

```
#7A6F6C   獎勵頁小熊心情那一行 → 換回 var(--gray-1)
```
📌 它的值幾乎就是**舊的** `--gray-1`（`#7A7572`）——
也就是有人手調了一份出來，然後 token 加深時它沒有跟著動。
**那正是「第二份定義」最典型的樣子：不會報錯，只會慢慢跟本尊分家。**

⏳ 還有一個沒動：`#FAF5EF`（配桌的某個底）——
它與 `--milktea-light` 同值，可能是漏了沒換，但它是**底色不是文字**，
不影響對比，留給下次順手處理。

### ✅ 2026-09-09：白字膠囊也改用 `--accent-deep`（v1.9.0）

11 顆配白字的膠囊（通知紅點的數字、「每天開桌」那類），
底從 `--accent` 換成 `--accent-deep`：**4.00 → 6.02**。

同批把 `--accent-text` 改名為 **`--accent-deep`** —— 理由見規則二。

🔴 **而我一開始建議「先不動」，那個判斷是錯的**，使用者反問「為何不直接做」。
逐條檢討：

| 我當時的理由 | 為什麼站不住 |
|---|---|
| 「4.00 vs 4.5 差一點」 | **門檻就是門檻**，而這份文件整篇在講「內建比事後修便宜」—— 留 12 筆已知失敗與它矛盾 |
| 「徽章不是要讀的句子」 | 通知紅點的數字是**功能性文字**，客人要讀它才知道有幾則 |
| 「命名問題」 | ✅ 這條是真的，但那是**十分鐘的問題**，不是跳過的理由 |

🎯 **教訓：「差一點」不是理由，是「還沒想清楚代價」的說法。**
真正該講的是「改它要付什麼」，而那一算就發現只是十分鐘。

**最終成果：95 筆／20 組合 → 59／8 → 47／7。**
🎯 **剩下的 47 筆全部是那兩類刻意不修的**（`--gray-2` ×46、LINE 綠 ×1）
—— **可修的已經全部修完。**

📌 實機確認：進度條的填色**沒有**跟著變深 —— 那正是拆兩個 token 要保住的東西。

---

## 七、新增色票之前要回答的三個問題

1. **它是文字還是填色？** 兩者都要就該拆兩個 token（規則二）。
2. **它會站在哪些底上？** 全部量一次，**最難的那一個決定值**。
3. **既有的色票裡真的沒有嗎？** 硬規則 13.5 的同一條 ——
   挑不到就提出來討論，不要因為「差一點」就加一階。
