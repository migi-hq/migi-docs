# 決策紀錄（合併版）

> **這是什麼**：已拍板的架構與產品決策，含當初的理由。**理由比結論更難重建，所以優先記理由。**
> **什麼時候讀**：要動相關設計前先看一眼，避免推翻已有結論或重走冤枉路。
> 被推翻的舊決策以「已作廢」標示 —— 知道「什麼被否決過」跟知道結論一樣重要。
>
> **來源**：2026-08-14 由三份 `決策紀錄.md` 合併（原檔保留於 `_inbox/`）。
> 三份涵蓋不同領域與時期，其中金流一節有直接衝突，裁決記在最後一節。

---

## 一、品牌與定位

- 品牌 **MIGI 麻將連鎖**。CIS：蜜桃粉 `#FAD6DC`、櫻花粉、淺灰、奶茶 `#EFE3DA`、白；小熊吉祥物。
  **是精品粉，不是少女粉。**
  > 奶茶色原本記為 `#DBC3B5`，與實作用的 `--milktea #EFE3DA` 不符。
  > 2026-08-15 拍板：**以 `#EFE3DA` 為準**，`#DBC3B5` 作廢。
  > 完整色票見 `docs/04-設計系統/設計Token規範.md`。
- 定位：「乾淨、安心、有質感的麻將空間，女生也愛來」。
  男 8 女 2，女性友善是吸客鉤子，**但女性是真核心客（紅線），不是誘餌**。
- 核心產品 = **成桌率**（99% 的動機是「有牌打」）。
- 護城河四層：① 數據累積 ② 網路效應（人鎖人）③ 品牌信任 ④ 平台化。

---

## 二、命名與版本線

**兩條平行的 milestone 線，永不撞號：**

- `M` 系列 = POS／營運端：M1 錢包（已部署）、M2 開收桌、M3 CRM、M4 段位 Elo
- `MA` 系列 = 會員 App 端：MA1 社交與配桌

起因：最初把 App 的 schema 取名 M1，撞到已部署的錢包 M1，故改前綴分流。

repo 命名為 `migi-admin` 而非 `migi-hq` —— 後者會與 GitHub org 名重複，變成 `migi-hq/migi-hq`。

---

## 三、金流與計價（2026-07-12 改版）

### 現行決策

1. 所有商品售價單位為**元**（新台幣），顯示 `$100`
2. 付款方式：**純點數 / 純現金 / 點數+現金混合**
3. 1 點 = 1 元。點數從「計價單位」變成**付款方式（tender）之一**
4. **檯費 = 場地使用費，可以直接收現金**

### 為什麼改

牌桌上不能見的是**賭資現金**（輸贏籌碼），而場地使用費是正常的服務收費，收現金天經地義。
這釐清了紅線的真正邊界：**紅線是「牌桌賭資不見現金 + 點數不可兌現」，不是「店裡所有收費都不能碰現金」。**

### ⚠ 已作廢的舊規則（不要走回頭路）

- ❌「桌面不見現金，一律走點數」（cash→points first）
- ❌「所有付款先轉點數，無一例外」
- ❌「現金不足 → 提示先儲值」

### 現行金流鐵則

1. **一律會員，無例外** —— 純現金客也要建會員。
   舊鐵則真正想守的其實就是這件事（消費累積進 CRM、牌咖關係、店員業績歸因、段位掛人），
   現在直接守它，不繞點數這條路。
2. **一律入帳，現金也不例外** —— 每筆收款都要記 `order_payments`，沒有私下收現金的路徑。
3. **賭資不上桌** —— MIGI 只收場地費與商品費，不經手、不記錄、不抽成牌桌輸贏。

### 對系統的影響

`checkout_tx` 本來就是 tender 模型（應付金額 → 點數折抵 → 現金補足），**天生支援新模式**，
後端幾乎不用改。主要工作在 POS UI（付款方式選擇、實收找零）與開桌流程。

### 貨號採三段式（2026-08-07）

`類別-子類-品項`，例如 `SVC-TBL-M3`、`FNB-MEAL-DUMP`、`FNB-DRK-MILKTEA`。

真正的價值在**子類那一層** —— 同類別內需要分開看的維度（飲料 vs 餐點的毛利結構完全不同）。
有了它，`sku LIKE 'FNB-DRK-%'` 就能算飲料營收，不必另建分類對照表。

**演變過程**（避免走回頭路）：
1. 最早：貨號隨手取（`FEE-100`、純數字 `123`），與 category 對不上
2. 一度定：兩段式 `SER-001` 流水號 → **對檯費是錯的選擇**，流水號會把
   「配桌/包桌 × 將數/時段」的結構丟掉，`SER-002` 看不出是什麼
3. 定案：三段式，檯費用語意化第三段（M2/M3/MID/P02…），餐飲零售也用語意縮寫

**`SVC-TBL-MID` 為什麼獨立，不共用 `SVC-TBL-M2`**：兩者同價 100 但性質不同
（預定 2 將 vs 半路入場的低消）。共用的後果是：日後 2 將調價，中途加入被迫跟著改；
報表分不出「多少人是中途加入」—— 而那是評估配桌成桌品質的關鍵指標。

**已知代價**：字串比對會靜靜漏資料（打成 `FNB-DRINK-` 就從報表消失且不報錯）；
改貨號會改變歷史報表（`order_items` 不存 sku）。
日後若子類報表變關鍵，加 `products.subcategory` 欄位，貨號純當標籤。
頂層分類**不需要**用 LIKE —— `order_items.kind` 結帳時就存了 fee/fnb/goods，直接用更快。

### `unit_points` → `unit_price`（2026-07-12）

改元計價後欄位叫 points 名不符實，會長期誤導。趁測試階段、無真實訂單資料時一次改對。
`order_items` 原本 `unit_points`（not null 主欄）與 `unit_price`（相容欄）雙欄重複存同值，
已淘汰前者、後者轉正為 not null，`checkout_tx` 同步只寫 `unit_price`。

---

## 四、會員分級雙軌

- **會員等級**（消費累積，**不含儲值**）：珍珠奶茶 → 焦糖布丁 → 提拉米蘇。門檻 0 / 10k / 50k（待定）
- **段位**（打牌 Elo，小熊）：銅牌熊 → 銀牌熊 → 金牌熊 → 白金熊 → 鑽石熊 → 大師熊，
  各 I–IV 小級，可掉段，起始約 1000。大師熊需賽事認證
- 雀神熊 = 賽季頭銜（一季一位）+ 名人堂，另設女子組平行頭銜
- UI 兩者並列顯示，如「焦糖布丁 · 金牌熊 II」
- **會員端 App 禁用數字段位（三段／四段），一律用小熊**

> 註：資料庫 `members_tier_chk` 另有第四級 `chef_special`，但被舊約束 `members_tier_check` 擋住
> 寫不進去，修正 SQL 見 `sql/pending/`。

---

## 五、開桌與收費流程（2026-08-07）

| 決策 | 理由 |
|---|---|
| **收費一律委派 `checkout_tx`** | 券折抵、等級折扣、混合付款、冪等、五表寫入都已實作完整。自己重寫等於兩套邏輯並存，一旦不一致帳就出問題（草稿版重寫時完全漏掉「券」） |
| **兩步制**：開桌不收費，每來一人才收 | 對應櫃台實務 —— 麻將要等人到齊，客人是陸續報到的 |
| **收費時機：開桌即收**，不等收桌 | 佔位就付，保護成桌率 |
| **場次不設 `playing` 狀態** | 用 `activated_at` 是否有值區分「收費報到中／已開打」。不必擴充 status 約束，而 `activated_at` 本來就要記（日後接電控用） |
| **`org_id` 由桌位反查，不接受呼叫端傳入** | 函式是 SECURITY DEFINER（繞過 RLS），若信任呼叫端傳來的 org_id，任何登入者都能操作別的租戶 |
| **冪等鍵用正式欄位 + 唯一索引** | 草稿版借用 note 欄位（無約束、無索引），並發下兩個請求可能同時查不到而開出兩張桌 |
| **冪等鍵帶入座序號** | 同一人退出後再加入是新的收費事件，不能被當成重複請求 |
| **互黑檢查只警示不阻擋** | 系統處理標準，人處理例外 —— 店員保有最終裁量權 |
| **不用 `exception when others`** | 會連程式 bug 一起吞掉，除錯極痛苦 |

**開桌方式**：櫃台報到 + 雙軌開桌（滿四自動／店員手動例外）。
身分雙軌：LIFF 掃會員碼／報手機／店員代開（`line_user_id` 留空）。
識別用 **LIFF 掃會員碼**，不做實體卡、不做人臉。
員工後台採**購物車式開桌**：選空桌 → 連續掃會員進明細 → 選積分級距 → 合計 → 一鍵開桌結帳；
餘額不足擋下。

**收桌觸發三項更新**：消費累積 / 段位 Elo / 店員業績。（`settle_session_tx` 尚未實作）

**代付**：檯費份數 = 自己 1 份 + 代付人數。被代付者仍建立 `session_players` 記錄，
但 `charged_points = 0`、`paid_by = 付款人`。消費金額與發票都歸付款人 —— 誰出錢，帳就是誰的。

---

## 六、配桌、牌咖、黑名單（MA1-C）

### 配桌

- **成桌時間錨點 = `play_at`**（開房者指定的預定開打時間），不是「開房後 N 小時過期」
  - waiting 到 `play_at` 仍未滿員 → `expired`
  - **已成桌永不自動解散**；沒到的人由店員手動標 `no_show`
  - 理由：每個會員到店路程不同，用固定倒數對住遠的人不公平；「兩點半開打」是所有人共同的錨
- **房主退房 → 轉移給次早加入者**，不解散（對其他等待者公平）。房內剩 0 人才 `cancelled`
- **併發控制**：`join_match_queue_tx` 用 `SELECT ... FOR UPDATE` 鎖房，處理「兩人搶最後一位」
- **即時更新過渡方案**：先用 5 秒輪詢；等 LINE 真登入（authenticated）後再換 Supabase Realtime。
  理由：Realtime 走 RLS，而現階段 anon 被全擋
- **配桌等待層與計費桌分離**：App 的 `match_queues`（還沒入座）
  vs POS 的 `table_sessions`（已入座計費），用 `matched_session_id` 銜接

### 牌咖

- **邀請制**：單向發起 → 對方確認才成立（不是「互相加碰巧配對」）
- **拒絕無痕**：拒絕方跳 toast，發起方**完全收不到任何通知**
- **接受後**寫 `mahjong_buddies` 兩筆互指，並回通知給邀請方
- **不加防騷擾冷卻期**（被拒後可再邀）—— 已拍板不做
- **解除牌咖**：靜默雙向軟刪，對方不通知

### 黑名單

- **單向紀錄、雙向生效**：A 黑 B → 兩人互相看不到對方的房、互相進不去。
  只擋單向會讓 B 照樣加入 A 的房，形同虛設
- **訊息模糊化**：被擋一律顯示「此桌已無法加入」「無法邀請此牌咖」，與桌滿／解散同語氣，
  **永不洩漏黑名單存在**。理由：MIGI 是實體店，客人見得到面，資訊洩漏會在店裡引爆衝突
- **黑掉牌咖 → 自動解除牌咖關係** + 作廢未回覆邀請 + 清理相關通知

---

## 七、人機協作模型

**App 管撮合意圖，店員管現場執行。** 不追求線上全自動完美對齊，因為最後一哩有店員兜底：

- 店員可對未滿房**強制成桌 + 代打補位**讓桌準時開
- 店員／房主可改 `play_at`（喬時間）並通知全員
- 代打局的店員成績 M4 結算時排除；被人工介入的桌，M3 分析「自然成桌速度」時要排除

---

## 八、總部後台（HQ）

| 決策 | 內容 |
|---|---|
| 系統邊界 | HQ（`hq.migi.tw`）與 POS（`pos.migi.tw`）是**兩個獨立前端，共用同一個 Supabase**。HQ 管跨全體建檔與策略（全集團視角），POS 管單店當下營運 |
| 「店長 vs 店員權限」 | 那是 POS 內部的角色差異，與 HQ 無關 |
| 登入 | Supabase Auth Email + 密碼，第一個管理員帳號在 Dashboard 手動建 |
| 權限 | 第一版只做「總部管理員」單一角色（`role='hq'`） |
| 商品定價 | 現階段全集團同價，但表結構已預留 `store_id`，未來要分店價不必改結構 |
| 模組優先序 | 第一波（解鎖 POS）：商品管理、券管理 ／ 第二波：會員、報表 ／ 第三波：門市、加盟、權限 |
| UI 風格 | 白底灰框為主體，CIS 粉色用於選中態（**粉底黑字**，非桃紅字）。後台每天看資料，清晰優先 |

---

## 九、技術棧與基礎設施

- 前端 Vite + React 純前端 SPA → Cloudflare Pages
- 後端 Supabase（DB + Edge Functions + Storage + Auth）
- 程式碼**只放 GitHub 一處**，Cloudflare 自動從 GitHub 抓取部署（非兩邊各存一份）
- 會員通路走 LINE / LIFF
- Supabase 一律 **Dashboard 操作**，不用 CLI
- 資料分工：會員資料存 Database，檔案（照片）存 Storage，網址存回 Database 欄位（如 `avatar_url`）

---

## 十、帳號與網域啟動順序（雞生蛋解法）

1. 開專用 Gmail 當過渡信箱
2. 用它註冊網域商（Gandi）、買下網域
3. 開 `admin@` / `info@` 等公司信箱
4. **回頭把網域商帳號聯絡信箱改成 `admin@`**（轉正）
5. 之後所有平台一律用 `admin@` 註冊（綁職能不綁人，開 2FA）
6. 過渡 Gmail 留作緊急復原

> 個人信箱只是啟動鑰匙，用完功成身退；鑰匙圈最後落在 `admin@`。
> `migi.com` / `migi.com.tw` 已被佔用，改用 **`migi.tw` / `migi.app`**。

---

## 十一、空間與成本（15 坪首店）

- 15 坪狹長型（面寬 4.5m × 進深 11m）可放 **6 桌**自動麻將桌；主走道 0.9m
- 動線：入口 → 櫃台報到 → 主走道入座 → 最裡茶水／廁所
- 開辦成本約 **116–191 萬**（建議抓 130–180 萬）；每月固定成本約 22–25 萬
- 自動麻將桌一台 1.5–2 萬（6 台 9–12 萬）；品牌／系統／開辦雜支壓在 5 萬內（系統自建、行銷自己來）
- 總資金建議準備 220–330 萬（含 3–6 個月週轉金）
- **不能省的：空調／除菸／軟裝** —— 這是女性友善的命脈

---

## 十二、設計語言與語言鐵則

- 會員端 App、員工後台、發券後台**一律白底系**
- 簡報的深褐黑底是**投影片展示舞台背景**，不是 App 實際底色。
  對齊簡報時只對齊「白卡內容的設計語言」，不要把背景做成深色
- 卡片語言：三圓點卡頭、淺灰數據小卡 `#F8F7F6`、蜜桃粉強調格、奶茶色膠囊標籤、深色大圓角按鈕
- **版本號顯示**：照業界慣例（LINE、銀行 App）放設定頁底部，不做隱藏彩蛋

**語言鐵則**：繁體中文；全程台麻用語，**日麻用語禁用**（不用立直／點棒／ドラ）。
台麻 16 張；北部台有花（144）、南部台無花（136）；**高雄 = 南部台無花**。牌規需可設定。

---

## 十三、測試資料

- **不刪、用 `is_test` 標記隔離**（帳本 append-only 刪不掉，且刪帳本是財務禁忌）
- 標記欄位選 `is_test` 而非借用 `line_user_id` 前綴：後者是 LINE 登入的正職欄位，
  兼差當標記遲早出事
- 統計一律走 `v_real_*` 檢視表

---

## 十四、刻意「晚做」的項目（正確判斷，非偷懶）

- **TypeScript**：等後端資料結構穩定後再遷移
- **自動化測試**：等功能穩定、上線前補關鍵路徑
- **React Query / 樂觀更新 / 三態骨架屏 / 多裝置寫入衝突處理**：等上線有真實流量再補

---

## 十五、待拍板

| 項目 | 現況 |
|---|---|
| **當日暢打（`SVC-TBL-DAY` 300）怎麼收** | 商品已建但無程式路徑（`open_session_tx` 只接受 matched/private）。選項：加第三種模式 daypass ／ 當一般商品在 POS 賣（**傾向這個** —— 「今天不限次數」與「這桌收多少」是不同維度）／ 暫不處理 |
| **檯費價格正式確認** | 配桌 2 將 100／3 將 150、中途加入 100、暢打 300、包桌 400/600/800 —— 這些數字取自測試資料與舊規劃文件，尚未正式拍板 |
| **打 1 將怎麼算** | `open_session_tx` 目前只接受 2 或 3 將，實務上打 1 將者以 2 將場次開桌 |
| **配桌 vs 包桌的判斷準則** | 客人自己揪四人卻用系統配桌功能，算哪種？影響收費（配桌 per-head 4 人 = 600 vs 包桌 2 小時 = 400），**店員每天會遇到，需要明確準則** |
| **進貨成本怎麼算** | 業界標準是「移動加權平均」（每次進貨重算平均成本存回 `unit_cost`）。需要做「進貨」功能才能落地，現階段可先不追成本 |

---

## 十六、上線前必做（附 2026-08-14 驗證狀態）

| # | 項目 | 狀態 |
|---|---|---|
| 1 | **移除 migi-web wallet 的測試券 fallback** | ❌ **仍在** —— [wallet.jsx:26-30](../../migi-web/src/pages/wallet.jsx) 寫死三張假券（新會員免費奶茶、檯費 9 折券、儲值回饋 50 點），`_realCoupons.length > 0 ? _realCoupons : [...]`。沒券的會員一定看到，而且畫面標「結帳時可用」，拿到 POS 核銷不了。`valid_to` 還是 2026-07-28（早就過期） |
| 2 | **清測試資料** | 未做。**含軟刪除的一起硬刪**（軟刪除是營運中機制，清庫是一次性徹底清空）。依外鍵順序：`order_items` → `orders` → … → `products`。再砍測試 org（`11111111-…`），走正式建店流程生正式 org |
| 3 | **公司登記**（統編、營業登記） | 未做。這是正式上線的前置關卡 —— 正式 org 要等公司成立才有意義 |
| 4 | **`products.js` 的貨號自動生成要改** | ❌ **仍在，且比原記載更嚴重** —— [products.js:18-34](../../migi-admin/src/lib/products.js) 的 `nextSku` 產生兩段式流水號，且前綴表寫的是 `SER-`／`FNB-`／`MER-`，但資料庫實際用 `SVC-TBL-*`。它取「貨號尾端數字最大值 +1」，掃到 `SVC-TBL-P24` 會得 24 → 產出 `SER-025`。**只要有人用後台新增商品，就會生出與三段式體系不相容的貨號** |

---

## 十七、合併時的衝突裁決（2026-08-14）

**金流：「一律先轉點數」vs「可收現金」** —— 兩份文件直接衝突。
採用 2026-07-12 那版（元計價 + 混合付款、檯費可收現金），因為它有明確日期、
列出了作廢清單、並說明了紅線邊界的重新界定。另一份是改版前的舊描述。
**舊規則已移入本文第三節的「已作廢」區塊，不要再依它設計 UI 或 RPC。**

**檯費 SKU 文件指向** —— 舊文件寫「見 `table-fee-sku-catalog.md` 為最新」，
該檔名不存在，現行文件是 `docs/02-POS與開桌/檯費SKU商品制.md`。已更正。

---

## 十八、高雄自由店的性質（2026-08-15 裁決）

**背景**：兩份文件互相矛盾。

- `sql/applied/門市真實資料.sql` 寫著高雄自由店是「**第一家真門市**」，
  id 維持 seed 的 `22222222-…` 不換隨機 UUID —— 理由是已有 9 張表以
  `on delete restrict` 綁著它，換 id 要做搬遷手術、風險高
- 本文第十六節「上線前必做」#2 寫著要**砍掉測試 org（`11111111-…`）**、
  走正式建店流程生正式 org

砍掉 org 的話，那間「真門市」跟著陪葬，兩者不可能同時成立。

**裁決：採用「砍掉測試 org」那條。**
整個 org `11111111-…` 都是測試資料，底下沒有任何正式門市。
高雄自由店是 M0 seed 的示範店，後來被改成寫實的名稱與地址，但性質仍是測試資料。
→ `門市真實資料.sql` 的「第一家真門市」說法**作廢**，該檔僅保留為歷史紀錄。

**但它繼續當測試店用，資料照常累積**：
`list_stores_tx` 不看 `is_test`（2026-08-15 以 `pg_get_functiondef` 查證），
所以標成測試之後門市選單照常出現、14 張桌照常可用，
只是被 `v_real_*` 排除在營運統計外，之後產生的訂單與場次也會自動標成測試。

修正 SQL：`sql/pending/2026-08-15_高雄自由店標記為測試.sql`

**這件事的教訓**：`is_test` 由觸發器依**門市**帶入，不是依會員。
所以「在哪間店測」決定了資料會不會混進營運數據 ——
而唯一沒標記的那間，恰好就是所有開發測試都在用的那間。
過濾邏輯等於反向運作，六間示範店被濾掉、真正的測試資料被留下。
