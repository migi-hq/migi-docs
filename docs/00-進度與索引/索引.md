# MIGI 文件索引

> **這是什麼**：`docs/` 的導覽圖。
> **什麼時候讀**：開新對話接手、或不確定某個主題該看哪一份時。

---

## 先讀這個順序

| # | 讀什麼 | 為什麼 |
|---|---|---|
| 1 | 根目錄 `CLAUDE.md` | 硬規則 + M2 現在做到哪。**每個 session 自動載入，它才是進行中工作的唯一真實來源。** |
| 2 | `01-資料庫/db-現況快照.md` | 線上資料庫的實際欄位、RPC 簽名、CHECK 約束。動 schema 前必讀。 |
| 3 | `08-決策與踩坑/決策紀錄.md` | 已拍板的決策，不要重新討論。 |
| 4 | `08-決策與踩坑/踩過的坑.md` | 遇到怪問題（白屏、快取、部署）先查這份。 |
| 5 | `00-進度與索引/里程碑總路線圖.md` | M0~M8 ＋ MA 系列的階段地圖。**M 編號以它為準。** |
| 6 | `00-進度與索引/待辦與未定案.md` | 尚未排程的 backlog、未拍板事項、跨階段阻礙。 |

---

## 各資料夾放什麼

| 資料夾 | 內容 | 該夾的權威檔 |
|---|---|---|
| `00-進度與索引` | 導覽、里程碑、backlog | 本檔、**`里程碑總路線圖.md`**（M 編號權威）、`待辦與未定案.md` |
| `01-資料庫` | 表結構、RPC、貨號規範 | **`db-現況快照.md`** = 事實層（線上實況、簽名、約束）<br>`資料模型設計說明.md` = 欄位語意與設計取捨<br>`RPC職責與設計.md` = 每支函式做什麼、能不能用<br>`資料架構基石規範.md` = 22 項動工守則 |
| `02-POS與開桌` | 開收桌流程、計費、POS 規劃 | **`開桌結帳流程規格.md` v2.0**（含金流鐵則三條）|
| `03-會員App與社交` | 牌搭、配桌、分級、優惠券 | `會員互動與留存系統規格.md` |
| `10-牌譜與AI辨識` | 牌譜資料中台、鏡頭安裝、AI 辨識、牌面編碼、裝潢預留、硬體部署 | **`牌譜成績資料中台技術設計.md` v0.4**（其餘幾份都引用它的章節編號）<br>單桌實體安裝看 `牌譜鏡頭安裝規格.md`；多桌與加盟看 `AI硬體部署與產品分級.md` |
| `04-設計系統` | 色彩 token、元件、文案語氣 | **`設計Token規範.md`**（三端共用，要用顏色先查它）|
| `05-埋點分析` | 事件字典、報表規格 | `埋點架構與缺口清單.md` |
| `06-架構藍圖` | 長期技術與後台架構 | `整合系統開發藍圖.md`（M0–M9 里程碑在第 6 節）<br>⚠ v0.4，**只能當長期方向參考**，M0–M2 實作方式已改，檔頭有對照表 |
| `07-營運商業` | 開店成本、菜單、獎金、空間 | `第一階段落地計畫.md` |
| `08-決策與踩坑` | 決策與踩坑（各已合併為單一檔）| 兩份都是 |
| `09-環境流程` | 部署、Git 雙帳號、測試帳號 | `部署與環境設定.md` |
| `_資產` | 非文章的東西：原型 HTML／程式、圖檔、簡報、試算表 | `POS-UI設計基準-v47.html`<br>`牌譜原型-手牌前後比對/`（Python 邏輯沙盤）|

---

## 幾個容易搞混的地方

**進行中的待辦看 `CLAUDE.md`，不要看這裡的檔案。**
`待辦與未定案.md` 只放「還沒排進 M2、或需要拍板才能動」的項目。
兩邊刻意不重複 —— 待辦同時寫兩處必然會漂移，這個專案已經吃過這個虧
（2026-08-15 整併前，同一份待辦散在三個檔案，三份的完成狀態互相矛盾）。

**`sql/applied/` 不是線上現況的鏡像。** 那是「當時交付的版本」，之後可能又改過而沒留檔。
改既有函式一律先 `pg_get_functiondef` 撈線上版（`CLAUDE.md` 硬規則 3）。

**程式碼不放在 `00`～`10` 那些文件分類裡。** 三個 repo 才是唯一真實來源，
舊版 jsx 放進文件只會造成版本漂移 —— 那正是這個專案反覆吃虧的地方。

**可執行但沒有正本的東西一律放 `_資產/`**，底線前綴就是在標「這不是文件分類」。
那裡本來就有 `POS-UI設計基準-v47.html` 這種可執行的原型，
2026-08-17 又加了 `牌譜原型-手牌前後比對/`（Python 邏輯沙盤）。
判準很簡單：**在別處有正本的程式碼一份都不要進來，沒有正本的放 `_資產/`。**

---

## 版本沿革

- **2026-08-14** 六個對話的 125 份檔案整合進 `docs/`，分十類。原始檔封存於 `_inbox/`。
- **2026-08-15** 文件合併完成，70 → 61 份：
  - 索引與待辦 7 → 2 份。
  - 刪除兩份 `_pack6` 舊版（基石規範 v1.0 被 v1.2 取代；
    開桌結帳流程 v1.0 的金流鐵則已被 v2.0 推翻，留著會被照做）。
  - `01-資料庫` 四份重疊文件（`資料表結構速查` / `資料表總覽` / `資料模型與RPC` /
    `後端RPC清單`）併為 `資料模型設計說明` + `RPC職責與設計`，
    事實層一律讓給 `db-現況快照.md`。
- **2026-08-17** 新增第十一類 `10-牌譜與AI辨識`（11 份），並歸檔 POS 硬體採購：
  - 牌譜兩份升版並移出 `03-會員App與社交`：
    中台設計 **v0.2 → v0.4**（新增側面鏡頭 L2 路線、座位↔會員綁定、兩碼牌面編碼）、
    統計維度 **v1.0 → v1.2**（跟著 v0.4 更新 L2 分層，新增起手牌／先天牌力維度）。
    舊版已刪 —— 它們的 `tile` 編碼與 L2 前提都被推翻，留著會被照做。
  - 新增 `07-營運商業/POS硬體與採購規劃.md`。原始討論稿把後端邏輯層寫成
    Edge Functions，與現況（Postgres RPC 為主線）不符，已在該檔第 0 節列出四項更正。
  - Python 原型放 `_資產/牌譜原型-手牌前後比對/` —— **沿用既有慣例，不開新先例**
    （`_資產` 本來就放可執行的 HTML 原型）。「程式碼不進文件分類」那條線因此守住。
  - 牌譜 schema 放 `sql/_設計稿未落地/`。
- **2026-08-22** 新增 `10-牌譜與AI辨識/AI硬體部署與產品分級.md`，
  整理自外部文件《MIGI_AI_麻將桌硬體架構與配置規劃 v1.0》。
  只收既有沒有的部分（PoE 多桌架構、Home/Club/Pro 三級、私場加盟、SQLite 緩衝、
  推理頻率分級、五階段研發順序），重複的鏡頭分工與硬體選型不重抄。
  - 🔴 該文件的**側面鏡頭角度是錯的**（建議左前方/右前方低角度）——
    台麻手牌立起、牌面朝玩家胸口，那個角度拍到牌背。
    已在《牌譜鏡頭安裝規格》§3.2 就地標註，**以既有那份為準**。
  - ✅ 採納它的 **SQLite 本地緩衝**主張：牌譜是不可重建的資料，
    斷網漏掉的輪次補不回來且沒有任何症狀。《牌譜鏡頭安裝規格》§9 已更新。
