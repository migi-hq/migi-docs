# 身分綁定：所有情境與處理流程

> 2026-08-30 建立。動任何與「客人是誰」有關的東西之前先讀這份。
> 每一格都是查 `pg_proc` / `information_schema` 查證過的，不是設計稿。

## 這份文件要回答的問題

MIGI 的會員身分由**兩條線索**組成：

| | 來源 | 誰保證它是真的 |
|---|---|---|
| `members.line_user_id` | LIFF 的 `id_token`，Edge Function 驗簽後取得 | ✅ LINE |
| `members.phone` | **客人自己填的** | 🔴 **沒有人** |

🔴 **所有問題都源於第二行。** 手機在 MIGI 不只是聯絡方式，
它是**認回舊帳號的鑰匙**（`register_member_tx` 用它找人並綁定）——
而一把沒有人驗證過的鑰匙，就是一把公開的鑰匙。

---

## 表 A · 註冊／登入當下的每一種組合

進入點：LIFF → Edge Function `line-login` → `register_member_tx`。

| # | LINE 查得到 | 手機查得到 | 那個會員的 LINE 狀態 | 現在的行為 | 判定 |
|---|---|---|---|---|---|
| **A1** | ✅ 是他 | — | — | `existing_line` → 直接進 App | ✅ 正確 |
| **A2** | ❌ | ❌ | — | `created` → 建新帳號 | ✅ 正確 |
| **A3** | ❌ | ✅ | **還沒綁 LINE** | `rebound` → **直接把 LINE 綁進那個帳號** | 🔴 **洞** |
| **A4** | ❌ | ✅ | 已綁**別的** LINE | `line_conflict` | 🟡 不綁，但**會交出 member_id** |
| **A5** | ❌ | ✅ | — （沒送 line_user_id） | `existing_phone` | 🔴 **會交出 member_id** |
| **A6** | ❌ | 沒填手機 | — | `created`（手機空） | ⚠ 之後補填時會撞上同樣的問題 |

### 🔴 A3：帳號接管

```sql
update members set line_user_id = p_line_user_id
 where id = v_existing and line_user_id is null;   -- v_existing = 手機對得上的那個人
```

填一個**還沒綁 LINE 的舊客人的手機** → 你的 LINE 綁進他的帳號
→ 錢包、消費紀錄、等級、優惠券全部到手。

⚠ 唯一的護欄是 `line_user_id is null`，所以**暴露的正是「櫃檯註冊過但還沒用 LINE 的舊客人」**
—— 而那群人正是**有餘額的人**。

### 🔴 A4／A5：不用綁定就能看到別人的錢包

`line_conflict` 那段的註解寫著「不回傳對方的 line_user_id —— 那是別人的識別碼」，
**但它回傳了 `member_id` 與 `display_name`** ——
而在待辦 14 完成之前，**`member_id` 才是會員端的通行證**：

```
register_member_tx(org, '隨便一個暱稱', 對方的手機)  → member_id
get_wallet_tx(member_id)      → 餘額、優惠券、點數流水
get_my_orders_tx(member_id)   → 完整消費明細
```

🔴 **防到了錯的東西。**
📌 已修：`sql/applied/2026-08-30_註冊路徑停止洩漏會員身分.sql`
（收回 anon ＋ A4／A5 不再交出 `member_id`）。

---

## 表 B · 客人換東西的四種情境

| # | 情境 | 登入還通嗎 | 現在會怎樣 | 該怎麼處理 |
|---|---|---|---|---|
| **B1** | 老客人再來（同 LINE、同手機） | ✅ | A1 | — |
| **B2** | 全新客人 | ✅ | A2 | — |
| **B3** | **換 LINE 帳號**（手機沒變） | ❌ 認不出他 | 走 A3／A4 | 店員換綁 or OTP |
| **B4** | **換手機號**（LINE 沒變） | ✅ 通 | 登入沒問題 | 🔴 但資料庫的手機**改不掉**（`set_member_phone_tx` 不存在） |
| **B5** | **兩個都換** | ❌ 兩條線索都斷 | 建出**第二個帳號** | 🔴 只剩合併工具（待辦 15，不存在） |

### 🎯 B4 與 B5 是同一條因果鏈

```
B4 能不能更新手機  →  決定 B5 會不會發生
B5 一旦發生        →  只能靠店員 ＋ 合併工具（兩者都還沒有）
```

**「讓客人能改手機」不是小功能，它是唯一能預防 B5 的東西。**

---

## 表 C · 異常與惡意

| # | 情境 | 現在會怎樣 | 處理 |
|---|---|---|---|
| **C1** | 填別人的手機（對方沒綁 LINE） | 🔴 **接管對方帳號** | 堵 A3 |
| **C2** | 填別人的手機（對方已綁） | 不綁，但**拿到 member_id** | ✅ 已修 |
| **C3** | 只想看別人的錢包，連 LINE 都不用 | 🔴 直接叫 `register_member_tx` | ✅ 已修（收回 anon） |
| **C4** | **佔用一支還沒註冊的號碼** | 之後真正的機主註冊時會被綁進來 | 改手機必須驗證或走店員 |
| **C5** | 同一人同時在兩台裝置註冊 | ✅ 有處理（`v_cur_line = p_line_user_id` → `existing_line`） | — |
| **C6** | localStorage 裡有舊身分 | 🔴 **直接變成別人**，連註冊都不會出現 | 身分改吃 LINE（待辦 14） |
| **C7** | 客人清掉 App／換手機 | ✅ LIFF 重新登入 → A1 | — |
| **C8** | 暱稱冒充官方 | ✅ `display_name_reserved` 擋掉 | — |

### 🔴 C6 已經真的發生過

2026-08-29：創辦人在 LIFF 走註冊，**進去變成測試01**。
資料庫可以證明 `register_member_tx` 一次都沒被呼叫到 ——
不是「繼承」，是那台裝置的 localStorage 裡存著別人，
所以**連 LINE 授權那一步都不會出現**。

```js
App.jsx:95   if (!member) return <Register …/>      // member 來自 localStorage
```

⚠ 當下的解法是「個人設定 → 登出」，但那只是繞過。
**這個 App 的身分是 localStorage 說了算，不是 LINE 說了算。**

---

## 表 D · 店員端的補救工具

| # | 需求 | 工具 | 狀態 |
|---|---|---|---|
| **D1** | 換綁 LINE（B3） | `rebind_line_user_tx(org, member, line, staff_id)` | ✅ 函式在　🔴 沒有 UI、沒有店員登入 |
| **D2** | 改手機（B4） | `set_member_phone_tx` | 🔴 **不存在** |
| **D3** | 合併兩個帳號（B5） | — | 🔴 **不存在**（待辦 15） |
| **D4** | 找得到人 | `pos_search_members_tx` | ✅ 但只能搜暱稱／手機 —— B5 的客人兩個都可能對不上 |
| **D5** | 誰能按 | 需要 `p_staff_id` | 🔴 店員登入未做（待辦 20） |

🔴 **D1 有一個沒有人談過的缺口**：
POS **拿不到客人的 `line_user_id`** —— 它只存在客人的 LIFF 裡。
所以「店員換綁」不是「按一個鈕」，需要一個交換機制：

```
客人 App  →  按「櫃檯綁定」→ 顯示 6 碼（5 分鐘過期）
店員 POS  →  搜手機找到舊帳號 → 輸入那 6 碼
          →  rebind_line_user_tx(org, member, line_user_id, staff_id)
```

要做：`line_bind_codes` 表 ＋ 產碼／核碼兩支 RPC ＋ 兩端各一個畫面 ＋ 店員登入。
⚠ **唸 LINE 名稱給店員是不行的** —— `line_user_id` 不是名稱，而且名稱可以改。

---

## 表 E · 兩條路線的比較（上線前必須選一條）

| | 堵住 A3 | 舊客人綁得回來 | 需要什麼 | 長期成本 |
|---|---|---|---|---|
| **① 簡訊 OTP** | ✅ | ✅ **自助完成** | 簡訊商、`otp_codes` 表、防轟炸 | 🔴 **每則都要錢，永遠要錢** |
| **② 店員綁定** | ✅ | ⏳ **卡待辦 20** | 綁定碼表、店員登入、POS UI | ✅ 一次性工程 |

⚠ **兩邊的工程量其實差不多。** ② 看起來便宜是錯覺 ——
它的依賴（店員登入）比 ① 的依賴（接一家簡訊商）**更遠**：
店員登入還卡著 LINE channel、權限設計、待辦 21 的五項阻擋條件。

📌 ① 與 ② **不互斥**：日後接 OTP 只是把「店員按」換成「自己驗」。

---

## 🎯 現在就該做的（與上面那個選擇無關）

| | 為什麼現在做 |
|---|---|
| ✅ **收回 `register_member_tx` 的 anon**（已完成） | 攻擊門檻從「知道一支手機」提高到「知道手機 ＋ 有 LINE 帳號」 |
| ✅ **A4／A5 不再交出 `member_id`**（已完成） | 那是會員端的通行證 |
| ⏳ **堵 A3** | **今天零代價** —— 還沒有真的舊客人（4 個測試帳號 ＋ 創辦人）。<br>上線後才堵，影響的是每一個櫃檯註冊過的人 |

```
今天堵 A3：影響 0 人
上線後堵：影響每一個櫃檯註冊過的舊客人
```

⚠ 四個測試帳號日後要接 LINE，走 Dashboard 直接下 `rebind_line_user_tx`（service_role），不受影響。

---

## 📌 順帶記下來的兩個原則

**① 驗證（你是誰）與聯絡（怎麼找到你）是兩件事。**

| 手機拿來做什麼 | 需要驗證嗎 |
|---|---|
| 當身分 | ❌ 不用 —— LINE 已經驗過了 |
| 當聯絡方式（叫車、桌況、客訴） | ❌ 不用 OTP —— 填錯頂多打不通 |
| 🔴 **當認回舊帳號的鑰匙** | ✅ **一定要** |

📌 foodpanda／Uber Eats 台灣用 LINE 登入後也要填手機，但**那支手機不會認回舊帳號**
—— 所以他們不需要 OTP。星巴克、全聯是手機當主鍵 → 他們**一定**發 OTP。
**MIGI 現在是第三種，卻沒有驗證。**

**② 待辦 14（JWT）是根本解，但它不能單獨做。**
JWT 之後 `member_id` 不再是通行證，A4／A5／C3 自然消失。
⚠ 但待辦 21 的五項阻擋條件必須先完成，而其中一項（帳號合併）
**必須在 JWT 之前或同時做** —— JWT 上線那一刻就是
「LINE 帳號 ↔ member」正式綁定的時候，那時若已有重複帳號，綁定會固定下來。
