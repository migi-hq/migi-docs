# 個資盤點與隱私權政策要點

> 2026-09-04 建立。來源：`information_schema` 實查，不是憑印象列的。
>
> ⚠ **這不是隱私權政策本身，是寫政策之前的盤點。**
> 我不是律師，**條文要人審**。但「你到底收了哪些個資」這件事
> 只有查資料庫才知道，而**政策寫得對不對完全取決於這份清單完不完整**。
>
> 🔴 上線清單第 ⑦ 項。它**完全不依賴任何功能**，所以是八項裡
> 最早就能做完的一項 —— 而它是**法規要求**不是 LINE 的要求。

---

## 一、你實際收了什麼（`members` 44 欄，逐欄分類）

### 🔴 直接識別個人

| 欄位 | 從哪來 | 備註 |
|---|---|---|
| `line_user_id` | LINE 授權 | 🔴 **per-Provider 的永久識別碼** |
| `phone` ／ `phone_verified_at` | 客人填 ＋ 簡訊驗證 | 可直接聯絡 |
| `display_name` | 客人填 | 暱稱，非真實姓名 |
| `avatar_url` ／ `avatar_photo_path` | LINE 頭像／自己上傳 | 🔴 **自己上傳的可能是真實臉孔** |

### 🟡 個人屬性（行銷側寫用）

`gender`、`birthday`、`occupation`、`district`、`acquisition_source`、
`home_store_id`、`about`、`sched`、`style`

⚠ `birthday` 是**生日招待**的依據，不是可有可無的欄位 ——
政策要說清楚「不提供的影響」（個資法第 8 條第 6 款要求告知）。

### 🟡 行為與消費

`last_visit_at`、`visit_count`、`lifecycle`、`last_app_active_at`、
`tier`、`rating`、`rating_games`、`rank`、`likes_count`
＋ 另外幾張表：`orders` / `order_items` / `wallet_txns` / `topup_orders`
（**買了什麼、花多少、什麼時間在店裡**）、`app_events`（每一次點擊）

### 🔴 發票（法規強制保存）

`inv_type`、`inv_carrier`、`inv_donate_code`、`inv_tax_id`、`inv_title`
⚠ 這一組的**保留期限不由你決定** —— 稅法有規定，
所以「刪除權」對它是**有法定例外**的。政策要寫。

---

## 二、🔴 最容易漏掉的一項：店員備註

```
member_interactions.note   ← 店員手寫的內容
```

🔴 **客人完全不知道這個東西存在**，而《個人資料保護法》第 3 條給當事人
**查閱權與複製權**，且**不得預先拋棄**。

⚠ 也就是說：客人來信要求查閱他的個資，**店員備註在範圍內**。
→ 兩個實際後果：
- **政策要把它列進「個人資料之類別」**，不能只寫「消費紀錄」
- 🎯 **店員教育訓練要講**：那一欄寫的東西，有一天可能會被當事人看到。
  寫「客訴過冷氣太冷」沒問題，寫評價性的字眼會出事。

### 同一族的還有一個（尚未實作，但設計已定）

`member_availability` 的 `source='inferred'` —— **系統推斷的常來時段**。
CLAUDE.md 待辦 26 拍板「**A 行為推斷不對客人顯示，連他本人都不行**」，
理由是「測量行為本身會改變行為」。

⚠ 🔴 **那個決定與個資法的查閱權有潛在衝突**：
「不主動顯示」沒問題，但「當事人要求查閱時不給」不行。
→ M3 做推斷引擎時要一起想清楚，**不是做完才補**。

---

## 三、資料流到哪裡（個資法第 8 條第 4 款：利用之地區、對象）

| 對象 | 什麼資料 | 🔴 跨境？ |
|---|---|---|
| **Supabase** | 全部 | 🔴 **新加坡**（`ap-southeast-1`，2026-09-05 於 Dashboard 確認）<br>⇒ **確定是跨境傳輸，政策必須告知** |
| **LINE**（LY Corporation） | 授權時取得 `sub` / `picture` | 日本 |
| **簡訊商**（MAAC Go／漸強實驗室） | 🔴 **手機號碼** | 台灣 |
| **Cloudflare Pages** | 連線 IP、User-Agent（CDN log） | 全球節點 |
| **電子發票加值中心** | 發票資訊（待辦 13，還沒接） | 台灣 |
| GA4 / Meta | ⏳ **還沒開**（`toGA4` / `toMeta` 都是 false） | 🔴 開之前政策要先改 |

🎯 **最後一列是順序問題**：GA4 一開就是把行為資料送給 Google，
而政策沒寫的話那一刻就違規。→ **政策要先於 GA4**。

---

## 四、政策必須涵蓋的六項（個資法第 8 條，法定告知事項）

```
① 蒐集者名稱          ← 你的公司登記名稱（不是品牌名）
② 蒐集之目的          ← 會員服務／配桌／點數／發票／行銷
③ 個人資料之類別      ← 上面第一節那張表，含**店員備註**
④ 利用之期間、地區、對象、方式  ← 上面第三節，含**跨境**
⑤ 當事人得行使之權利  ← 查閱／複製／補充更正／停止蒐集處理利用／刪除
⑥ 不提供的影響        ← 例：不填生日就收不到生日招待
```

⚠ ⑤ 要**寫得出怎麼行使** —— 一個信箱或一個頁面。
🔴 **寫「請洽門市」不夠**：法定回應期限是 15 日（得延長 15 日），
而門市店員不會知道這件事。→ 需要一個**真的有人看的信箱**。

### 用哪個信箱（2026-09-04 定）

🔴 **2026-09-04 更正一個錯誤假設**：這一段第一版寫「轉寄到 `info@`」，
**而 `info@` 根本還不存在** —— 今天唯一的信箱是 **`admin@migi.tw`**。
（那份 checklist 的「先辦 Google Workspace／開 info@」還是未勾。）

📄 分工來自 `docs/07-營運商業/開店前主體與帳務Checklist.md` 第五節：
```
info@      對外總機（官網、名片、IG bio）      ← ⏳ 還沒開
admin@     註冊平台的鑰匙圈                    ← ✅ 唯一存在的，🔴 不該印在政策上
service@   客服（會員問題）                    ← 「展店階段再加」
no-reply@  系統自動信                          ← 只發不收
```

🎯 **今天最省的結構 —— 不用買任何東西：**
```
admin@migi.tw       ← 唯一真的收件匣（不變）
    ↑ 別名／轉寄（免費）
privacy@migi.tw     ← 政策上印這個
```
⚠ **只加 `privacy@` 一個就好。** `info@` 等真的需要對外總機時再說 ——
那份 checklist 把它排在「開店前必要」是為了名片與官網，
**跟隱私權政策無關**。

**怎麼加**（✅ 2026-09-04 確認已有 Google Workspace）：
```
admin.google.com → 目錄 → 使用者 → 點 admin@migi.tw
  → 使用者資訊 → 「別名電子郵件地址」→ 新增 privacy@migi.tw → 儲存
```
⚠ **不佔授權額度**（不用加購使用者），一個帳號最多 **30 個**別名。
⚠ 生效通常幾分鐘，官方說最多 24 小時 —— **設完寄一封測試信確認**。

### 🔴 為什麼不要直接印 `admin@`

那個地址是**註冊所有平台的鑰匙圈**（LINE / Supabase / Cloudflare / GitHub）。
印在公開的隱私權政策上，等於告訴別人「**這個地址值得攻擊**」。
⇒ 多一個別名的真正理由是**這個**，不是收信方便。

其餘兩個理由：
· 法定請求有 **15 日**期限，而收件者是 `privacy@` 的那幾封**一眼分得出來**
· 🔴 **政策上印的地址日後極難改**（名片、官網、App 內、已公告的政策本文）——
  別名讓「誰在看信」可以換，而**對外那一行永遠不用動**

### 🔴 順帶：既有文件自己的網域矛盾（要更正）

`開店前主體與帳務Checklist.md` 第五節裡**兩個網域混用**：
```
第 78、80 行   migi.tw        ← ✅ 實際在用
第 86–97 行   migi.com.tw     ← 🔴 跟現實不符
```
實際狀態：`admin@migi.tw` 已經是 GitHub 的 **Primary 且已驗證**信箱、
Supabase Auth 的唯一帳號（`2026-07-12` 建立）、以及 Git 的 commit 身分。
→ **以 `migi.tw` 為準**，那份 checklist 的 `@migi.com.tw` 要改掉。

---

## 五、⏳ 現在的狀態與該做的事

| | |
|---|---|
| App 裡的「隱私權政策」／「服務條款」 | 🔴 **兩列都是「即將推出」的 toast** |
| LINE Login channel 的 Privacy policy URL | 🔴 **空的**（optional，所以沒被擋） |
| 政策本文 | 🔴 **不存在** |

**動手順序（都不依賴任何功能，今天就能做）：**
1. ✅ **蒐集者名稱：`咪吉有限公司`**（2026-09-04 使用者暫定）
   🔴 ⚠ **但這一項有一個要人回答的前提：公司登記好了嗎？**
     個資法第 8 條要告知的是**蒐集者**，而如果法人還不存在，
     今天在收個資的主體就是**自然人**（負責人本人），
     那時政策上寫一個不存在的公司名是錯的。
     → **登記完成前先不要公告政策**，或由審閱的人決定怎麼寫。
     ⚠ 這正是「找人審」不能省的地方，不是我可以代答的。
2. ✅ **聯絡信箱：`privacy@migi.tw`** —— **別名，轉到 `admin@`**（今天唯一的信箱）。
   ⚠ 不用買、不用開新帳號；理由與設定方式見上一節。
3. ✅ **資料所在地：新加坡**（`ap-southeast-1`，2026-09-05 於
   Supabase Dashboard → Settings → General 確認）⇒ **跨境傳輸確定**。

   🔴 **這一格差點被我猜錯，值得記。**
   我先用唯讀 MCP 查 `inet_server_addr()` = `2406:da18:…`，
   認出是 APNIC 配給 AWS 的亞太區段，然後推論「東京」——
   **錯的，實際是新加坡。而那個推論看起來完全合理。**
   ✅ 沒出事是因為我把它標成「憑印象、要去 Dashboard 確認」而不是直接填。
   ⚠ **政策上的國名寫錯，跟沒寫一樣糟，而且完全沒有症狀** ——
     沒有人會發現，直到有人來查。同硬規則 3：只有權威來源算數。

   📌 順帶：**這一項原本的問法就是多餘的** ——
     「確認 region 決定要不要寫跨境」，而 **Supabase 根本沒有台灣 region**，
     所以「要不要寫」從來不是問題，只有「寫哪個國名」是。
4. ✅ **草稿已完成：`docs/09-環境流程/隱私權政策_草稿.md`**（2026-09-05）
   —— 十二節，六項法定告知事項齊備，含**店員備註**與**跨境**。
   🔴 **還沒審。** 稿裡標了三處送審重點（保存年限／未成年年齡／跨境措辭）
   與兩個要你填的空格（**公司登記地址**、**入場年齡**）。
5. 放上 `app.migi.tw/privacy`，接上 `profile.jsx` 那兩列
6. 填進 LINE Login channel 的 Privacy policy URL

⚠ **還缺一份服務條款**（點數效期／退款／停權／責任限制）——
  `profile.jsx` 那兩列是**兩個入口**，這份稿只解決其中一個。

🎯 **順序上還有一件不能反的**：GA4 一開就是把行為資料送給 Google，
政策沒寫的話那一刻就違規 ⇒ **政策要先於 GA4**（上線清單第 ⑧ 項）。
