# 部署與環境設定

> **這是什麼**：MIGI 各前端專案的部署方式、網域架構、環境變數。
> **什麼時候讀**：新建專案、部署更新、或部署後出問題時。
> 前提：作業方式是**在 GitHub 網頁上傳檔案**（不用終端機），Supabase 一律走 Dashboard。

---

## 一、專案版圖

GitHub organization：**migi-hq**

| repo | 網域 | 用途 | 狀態 |
|---|---|---|---|
| `migi-web` | **app.migi.tw** | 會員 App（LIFF endpoint 指這裡） | 已上線 |
| `migi-pos` | **pos.migi.tw** | 店員 POS | 已上線（LINE 登入 2026-09-04） |
| `migi-admin` | **hq.migi.tw** | 總部後台 | 已上線（LINE 登入 2026-09-05） |
| `migi-site` | **migi.tw** ＋ `www.migi.tw` | 官網（今天只有首頁 ＋ 兩份政策） | 已上線 2026-09-05 |

🔴 **`migi-site` 的部署設定跟另外三個不一樣**（純靜態，沒有 build）：
```
Build command            留空      ← 另外三個是 npm run build
Build output directory   /         ← 另外三個是 dist
環境變數                  不用設    ← 它不連 Supabase
```

> repo 名不必與子網域相同（`migi-admin` → `hq.migi.tw`）。命名避開 `migi-hq`，否則會變成 `migi-hq/migi-hq`。

### 規劃中／未拍板（2026-09-05 收攏，原本散在三份文件裡）

| 網域 | 用途 | 出處 | 狀態 |
|---|---|---|---|
| `table.migi.tw` | 桌邊平板 | `里程碑總路線圖.md:166` | 規劃 |
| `fc.migi.tw` | 加盟主後台（只看自店） | `里程碑總路線圖.md:246` | 規劃（M5 連鎖化） |
| `driver.migi.tw` | 司機後台（車隊接送） | `里程碑總路線圖.md:270` | 規劃 |
| `display` / `kiosk` | 看板、自助機 | 本表舊版 | 只有裸字，無完整網址 |
| ~~官網~~ | —— | —— | ✅ **2026-09-05 已上線，見上表** |

### ✅ 官網的網域已定：**apex `migi.tw`**（2026-09-05 拍板並上線）

以下是當天查證與決策的過程，保留供日後參考。

#### 🔴 在此之前它**從來沒有被決定過**

本表舊版把官網寫成裸字 **`site`**，跟 `table` / `display` / `kiosk` 擠在同一格。
🎯 **那三個都是店內裝置，官網被順手放進同一列，看起來是排版順手不是拍板。**

⚠ 全 repo grep 結果：`www.migi.tw`、`site.migi.tw`、`web.migi.tw`
**一次都沒出現過**；`migi.tw` 這個 **apex 現在指向哪裡也完全沒有記載**。

**建議用 apex `migi.tw`，不要用子網域**：

| | |
|---|---|
| 品牌門面 | 名片上印 `site.migi.tw` 很奇怪；`migi.tw` 最短最好記 |
| SEO | Google 把 apex 與子網域視為**不同網站** —— 官網放子網域等於把品牌權重分散 |
| 對照 | `pos` / `app` / `hq` 都是**功能性入口**，官網不是同一類 |

### 🔴 兩個沒有人標註過的矛盾

**① 官網的技術棧，兩份文件打架**
```
可擴張技術基礎建設藍圖.md:116   官網要 SEO/SSR ⇒ 另用 Vercel + Next.js
本檔（舊版）:18                 site 列在 Cloudflare Pages 的專案版圖表
```
⚠ 這對**純靜態的政策頁沒有影響**，但日後 apex 若改指 Vercel，
要記得 `/privacy` 與 `/terms` 在那邊也要存在 ——
**政策網址斷掉不會有人發現，直到有人去點。**

**② `migi.app` 是懸案**
`決策紀錄.md:300` 寫「`migi.com` / `migi.com.tw` 已被佔用，改用
**`migi.tw` / `migi.app`**」，但**後續所有文件只落實了 `migi.tw`** ——
`migi.app` 買了沒有、要拿來做什麼，一次都沒有下文。
（`開店前主體與帳務Checklist.md:208` 那一項還是未打勾的。）

---

## 二、新專案部署步驟（Cloudflare Pages）

**1. 建 GitHub repo**
- 在 migi-hq organization 下，設 **Private**
- **三個 Add 選項全部不勾**（README / .gitignore / license）——建全空的 repo，否則上傳既有專案會衝突

**2. 上傳檔案（網頁方式）**
- 空 repo 頁面點 **"uploading an existing file"**，或 Add file → Upload files
- 拖曳的是**資料夾裡的內容**（package.json、src/ 等），不是資料夾本身——package.json 必須在 repo 根目錄
- **不要上傳 node_modules**
- 若 `src` 資料夾拖不上去，分兩次：先傳根目錄檔案，再進 src 傳裡面的

**3. Cloudflare Pages**
- Workers & Pages → Create → Pages → Connect to Git → 選 repo

| 設定 | 值 |
|---|---|
| Production branch | `main` |
| Build command | `npm run build` |
| Build output directory | `dist` |
| Root directory | **留空**（package.json 在根目錄） |

**4. 環境變數（漏了會連不上資料庫）**

| 變數名 | 值 |
|---|---|
| `VITE_SUPABASE_URL` | Supabase → Settings → API → Project URL |
| `VITE_SUPABASE_ANON_KEY` | 同頁的 **anon / public** key |

⚠️ 要用 **anon key**，不是 service_role（那是機密，絕不能放前端）。所有 MIGI 專案共用同一組值。

**5. 綁自訂網域**
專案 → Custom domains → Set up a custom domain → 輸入子網域。因為 migi.tw 已在 Cloudflare，DNS 與 SSL 會自動設好。

**6. 若需要登入功能**
Supabase → Authentication → Add user → 填 email + 密碼 → **勾 Auto Confirm User**（否則要 email 驗證）。
**接著必須把該帳號加進 `staff` 表**（role='hq'），否則 RLS 會擋掉所有讀寫。

---

## 三、Vite 設定（必要）

```js
// vite.config.js
export default defineConfig({
  plugins: [react()],
  build: {
    target: 'es2015',   // ★ LIFF 舊 WebView 相容，不可移除
  },
})
```

`target: 'es2015'` **永久保留**——移除的話 LINE 內開啟會白畫面（見《踩過的坑》）。

---

## 四、字型

不要用 Google Fonts（曾造成 403）。CSS 直接以系統字型優先：
```css
font-family: -apple-system, "PingFang TC", "Noto Sans TC", "Microsoft JhengHei", sans-serif;
```
中文顯示幾乎相同，載入更快，也不依賴外部服務。

---

## 五、⛔ 快取處理

🔴 **2026-09-05 更正：這一條原本寫「不要建 `public/_headers`」，那是錯的。**

真正的禍首是 **`/*` 萬用比對**，不是 `_headers` 這個檔案 ——
v1 用了 `/*` 把快取標頭套到**所有資產**上，JS 模組因此回傳 HTML
→ MIME 錯誤 → 整頁白畫面。

⚠ 而照字面執行那條規則會禁止掉**現在正在跑的東西**：
```
migi-web/public/_headers    快取策略 v3（只指名 / 與 /index.html 與 /version.json）
migi-site/_headers          frame-ancestors（只指名 /privacy 與 /terms 那四條路徑）
```
📌 `migi-web` 那份自己的註解就寫著「**非萬用 `/*`，避免 v1 的資產 MIME 事故**」
—— 也就是**寫檔案的人知道真正的原因，而這份文件把它記成了另一件事**。

### ✅ 正確的規則

**`_headers` 可以用，但路徑要指名，不要用 `/*`。**
- ✅ `/`、`/index.html`、`/privacy`、`/terms.html`
- 🔴 `/*`、`/assets/*` —— 會套到打包後的 JS/CSS 上

⚠ 而且 `_headers` **不生效也不會報錯** ——
所以加了之後要**量 response header**（`fetch(...).headers.get(...)`），
不要只看畫面正常就當作成功。

快取問題改用：
- Cloudflare → 網域 → Caching → **Purge Everything**
- 靜態圖片用 query string 版本號：`/bear-lv1.webp?v=2`（換圖時覆蓋同檔名 + 版本號加一）
- Vite 打包的 JS/CSS 自動帶 hash，不用管

---

## 六、白畫面排查 SOP

1. 開 **乾淨部署網址**（`<hash>.專案.pages.dev`，每次 deploy 專屬、無快取）→ 判斷是伺服器版本問題還是快取問題
2. F12 Console 看紅字
3. Cloudflare → Deployments 確認最新一次是 Success
4. 確認 GitHub 上的檔案真的是最新的、且**放在正確路徑**（拆多檔時最常見的錯是 lib 檔位置放錯）
5. 若乾淨網址正常、但正式網域白畫面 → 純 CDN 快取，Purge Everything 即可

---

## 七、Supabase 操作原則

- **一律走 Dashboard**（SQL Editor / Table Editor），不用 CLI、不做本地部署
- 診斷用 SQL **一份只放一個 SELECT**（Editor 一次跑多個只顯示最後一個結果）
- 碰既有表之前，先查欄位與約束（見《db-現況快照》，欄位語意見《資料模型設計說明》）
- Project ref：`roksgepxxmcewlkshtzn`／org：`migi-hq`
- MIGI 測試 org_id：`11111111-1111-1111-1111-111111111111`（刻意設成整排 1 好記；正式上線會走建店流程重建）
