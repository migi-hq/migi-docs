# 進度與待辦

**【這是什麼】** 目前做到哪、下一步做什麼、以及刻意延後的項目清單。
**【何時讀】** 開新對話接手時第一份讀；決定下一個要做什麼時。

---

## Milestone 分線

| 線 | 範圍 | 狀態 |
|---|---|---|
| **M 系列**（POS/營運端） | M1 錢包 | ✅ 已部署（24 項測試通過） |
| | M2 開收桌 | 待辦 |
| | M3 CRM / 推播 / 作息推斷 | 待辦 |
| | M4 段位 Elo 結算 | 待辦 |
| **MA 系列**（會員 App 端） | MA1-A 會員資料 | ✅ 後端＋前端完成 |
| | MA1-B 牌咖與通知 | ✅ 後端＋前端完成（資料庫層已驗證通過） |
| | MA1-C 配桌＋黑名單 | 📐 設計完成，未實作 |

---

## 立即待辦

1. **跑 `SQL-待執行/MA1A-補改暱稱.sql`** — 修「換手機暱稱重置」的 bug（前端已接好）
2. **部署後四帳號實測 B 塊**：
   測試01 開 App 看到測試02 → 切測試03 邀測試01 → 切測試01 通知接受 → 切回測試03 看「已接受」+ 牌咖列表
3. **修通知「查看」的 bug**：只有 `buddy_ok` 用通用彈窗；`settle`（收桌）要維持跳收桌視窗
   （目前兩者都被改成彈窗，是錯的）
4. **進 MA1-C**：依 `SQL-待執行/MA1C-配桌黑名單設計稿.sql` 產出正式可執行版

---

## 已完成的基礎建設

- ✅ 埋點接後端（事件流 + 會員綁定 + 滲透率快取 + 日活視圖，真機驗證通過）
- ✅ 錯誤邊界 + 錯誤自動上報（白屏改顯示友善畫面，錯誤進 `app_events`）
- ✅ 版本注入（設定頁顯示 + 埋點帶 `app_version`）
- ✅ vendor chunk 分離
- ✅ 設計 token 集中（品牌色進 `:root`）
- ✅ 圖片改 Vite content hash 管理（根治換圖快取問題）
- ✅ build hash 改 hex（根治 LIFF 白屏）
- ✅ 測試帳號隔離機制（`is_test` + `v_real_members`）
- ✅ 門市真實資料（MIGI 高雄自由店）

---

## 刻意延後（時機到再做，不是遺漏）

**規模化前補、現在補浪費：**
1. **React Query / TanStack Query** — 目前每頁各自 `useEffect + fetch`，無快取共享。**這是目前離世界級最遠的一塊**，上線有流量後第一個補
2. **樂觀更新** — 按讚等操作即時回饋、失敗回滾
3. **資料層失敗接上 `app_error` 埋點** — 目前只 `console.warn`
4. **loading / empty / error 三態骨架屏** — 目前只有「有資料」和「預設值」兩態
5. **多裝置寫入衝突的系統性版本控制** — 目前只有點狀補丁（稱號聯集只增不減）

**等結構穩定：**
- **TypeScript** — 等後端資料結構穩定後遷移
- **自動化測試** — 等功能穩定，上線前補關鍵路徑

---

## POS 端需求清單（M2 要做）

1. 店員開桌選人時，名單含**互黑會員跳警示**建議分桌
2. 店員可對**未滿房強制成桌 + 代打補位**；`play_at` 可由店員/房主修改並通知全員
3. 代打局的店員成績，M4 結算時排除
4. 被人工介入的桌，M3 分析時排除

---

## 其他未決 / 待補

- **LINE Developers 帳號尚未申請** → LIFF 目前是模擬登入、RPC 開給 anon（上線前必須收斂成 authenticated，並加 rate limit）
- **成就系統**：UI 完整（成就牆、稀有度、隱藏成就）但全是假資料；「200 個成就」的規劃清單在此對話中**找不到**，需重新提供或重新規劃
- **排行榜**：目前 demo 假資料；接 M4 真實積分時**務必加 `is_test=false` 排除**
- **`stake_levels`（底注）**：目前前端寫死，C 塊配桌時需接真實資料
- **`public/default-avatar.svg`**：仍是舊做法（固定檔名），要換它時記得比照搬進 `src/assets/`
- **營收報表**：M1 錢包目前只有交易操作、無跨會員彙總報表；未來寫報表時務必 `from v_real_members`
